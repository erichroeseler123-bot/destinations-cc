{
  "version": "dcc-network-v1",
  "updatedAt": "2026-03-11T00:00:00Z",
  "satellite_id": "partyatredrocks",
  "allowed_surfaces": ["discovery", "monetized"],
  "surfaces": [
    {
      "surface": "discovery",
      "min_confidence": 0.7,
      "allowed_node_types": ["city", "scene", "operator", "event", "artist", "venue"],
      "required_fields": ["node_id", "slug", "name", "tags"],
      "required_edge_types": [],
      "allowed_provenance": ["upstream", "manual", "derived", "enriched"],
      "excluded_statuses": ["archived"]
    },
    {
      "surface": "monetized",
      "min_confidence": 0.85,
      "allowed_node_types": ["operator", "event", "venue"],
      "required_fields": ["node_id", "slug", "name", "status"],
      "required_edge_types": ["operates_in"],
      "allowed_provenance": ["manual", "upstream", "derived"],
      "excluded_statuses": ["archived", "paused"]
    }
  ],
  "local_overrides": {
    "max_nodes": 20000,
    "max_edges": 120000,
    "notes": "Music/event transport focus"
  }
}
