{
  "version": "dcc-network-v1",
  "updatedAt": "2026-03-11T00:00:00Z",
  "satellite_id": "welcome-to-alaska",
  "allowed_surfaces": ["authority", "discovery", "monetized"],
  "surfaces": [
    {
      "surface": "authority",
      "min_confidence": 0.75,
      "allowed_node_types": ["city", "attraction", "scene", "operator"],
      "required_fields": ["node_id", "slug", "name", "authority_url", "geo"],
      "required_edge_types": [],
      "allowed_provenance": ["upstream", "manual", "derived"],
      "excluded_statuses": ["archived"]
    },
    {
      "surface": "discovery",
      "min_confidence": 0.7,
      "allowed_node_types": ["city", "attraction", "scene", "event", "operator"],
      "required_fields": ["node_id", "slug", "name", "tags"],
      "required_edge_types": [],
      "allowed_provenance": ["upstream", "manual", "derived", "enriched"],
      "excluded_statuses": ["archived"]
    },
    {
      "surface": "monetized",
      "min_confidence": 0.88,
      "allowed_node_types": ["operator", "attraction", "event"],
      "required_fields": ["node_id", "slug", "name", "status", "monetized_targets"],
      "required_edge_types": ["operates_in"],
      "allowed_provenance": ["manual", "upstream", "derived"],
      "excluded_statuses": ["archived", "paused", "beta"]
    }
  ],
  "local_overrides": {
    "max_nodes": 30000,
    "max_edges": 160000,
    "notes": "Alaska tours and shore-excursion focus"
  }
}
