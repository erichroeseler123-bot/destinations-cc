--- app/new-orleans/components/NewOrleansChooser.tsx
+++ app/new-orleans/components/NewOrleansChooser.tsx
@@ -165,8 +165,9 @@
         <div aria-live="polite" className="w-full flex flex-col items-center">
           <h1 className={visualStyles.headline}>
             <span className={`${visualStyles.headlineLine1} ${visualStyles.accentFont}`}>LET’S FIND YOUR</span>
+            <span className={`${visualStyles.headlineLine2} ${visualStyles.displayFont}`}>PERFECT</span>
             <span className={`${visualStyles.scriptAccent} ${visualStyles.scriptFont}`}>New Orleans</span>
-            <span className={`${visualStyles.headlineLine2} ${visualStyles.displayFont}`}>PERFECT ADVENTURE</span>
+            <span className={`${visualStyles.headlineLine2} ${visualStyles.displayFont}`}>ADVENTURE</span>
           </h1>
           <DecorativeDivider />
 
@@ -215,7 +216,7 @@
 
       {view === "swamp-second" && (
         <div aria-live="polite" className="w-full flex flex-col items-center mt-12">
-          <h1 className={styles.secondaryTitle}>How do you want to explore the swamp?</h1>
+          <h1 className={`${styles.secondaryTitle} ${visualStyles.accentFont}`}>How do you want to explore the swamp?</h1>
           <ConnectedBoard>
             <NewOrleansChoiceCard
               mode="action"
@@ -241,7 +242,7 @@
 
       {view === "guided-categories" && (
         <div aria-live="polite" className="w-full flex flex-col items-center mt-12">
-          <h1 className={styles.secondaryTitle}>What sounds best?</h1>
+          <h1 className={`${styles.secondaryTitle} ${visualStyles.accentFont}`}>What sounds best?</h1>
           <ConnectedBoard>
             {CHOOSER_CATEGORIES.map((cat, i) => (
               <NewOrleansChoiceCard
@@ -262,8 +263,8 @@
 
       {view === "guided-preferences" && currentCategory && (
         <div aria-live="polite" className="w-full flex flex-col items-center mt-12">
-          <h1 className={styles.secondaryTitle}>Narrow it down</h1>
-          <p className={styles.supportingLine}>What kind of {currentCategory.title.toLowerCase()} experience are you looking for?</p>
+          <h1 className={`${styles.secondaryTitle} ${visualStyles.accentFont}`}>Narrow it down</h1>
+          <p className={`${styles.supportingLine} ${visualStyles.sansFont}`}>What kind of {currentCategory.title.toLowerCase()} experience are you looking for?</p>
           <ConnectedBoard>
             {preferences.map((pref, i) => (
               <NewOrleansChoiceCard
@@ -287,8 +288,8 @@
           {recommendation.fallbackMessage || !product ? (
             <div className={styles.matchContainer}>
               <span className={styles.matchLabel}>Coming Soon</span>
-              <h1 className={styles.matchTitle}>More Options on the Way</h1>
-              <p className={styles.matchReason}>{recommendation.fallbackMessage || "This category guide is being prepared."}</p>
+              <h1 className={`${styles.matchTitle} ${visualStyles.accentFont}`}>More Options on the Way</h1>
+              <p className={`${styles.matchReason} ${visualStyles.sansFont}`}>{recommendation.fallbackMessage || "This category guide is being prepared."}</p>
               <div className={styles.matchControls}>
                 <Link
                   href="/tours"
@@ -303,8 +304,8 @@
           ) : (
             <div className={styles.matchContainer}>
               <span className={styles.matchLabel}>Your New Orleans Match</span>
-              <h1 className={styles.matchTitle}>{product.title}</h1>
-              <p className={styles.matchReason}>{recommendation.explanation}</p>
+              <h1 className={`${styles.matchTitle} ${visualStyles.accentFont}`}>{product.title}</h1>
+              <p className={`${styles.matchReason} ${visualStyles.sansFont}`}>{recommendation.explanation}</p>
               <div className={styles.matchControls}>
                 <FareHarborBookingButton
                   productTitle={product.title}
--- app/new-orleans/components/NewOrleansVisual.tsx
+++ app/new-orleans/components/NewOrleansVisual.tsx
@@ -39,7 +39,7 @@
   return (
     <div className={styles.boardContainer}>
       {promptBanner && (
-        <div className={styles.promptBanner}>
+        <div className={`${styles.promptBanner} ${styles.accentFont}`}>
           {promptBanner}
         </div>
       )}
@@ -90,7 +90,7 @@
   const innerContent = (
     <>
       <div className={styles.illustration}>{props.illustration}</div>
-      <h2 className={`${styles.cardTitle} ${styles.accentFont}`}>{props.title}</h2>
+      <h2 className={`${styles.cardTitle} ${styles.displayFont}`}>{props.title}</h2>
       <p className={`${styles.cardCopy} ${styles.sansFont}`}>{props.desc}</p>
       <span className={`${styles.buttonPrimary} ${styles.sansFont}`}>{props.cta}</span>
     </>
--- app/new-orleans/components/newOrleansVisual.module.css
+++ app/new-orleans/components/newOrleansVisual.module.css
@@ -37,7 +37,7 @@
   background-position: center 20%;
 }
 
-/* Increased warm lantern depth, preserved deep black shadows */
+/* Darker central overlay, warmer amber side lighting, subtle plum tint in shadows */
 .heroOverlay {
   position: absolute;
   top: 0;
@@ -45,9 +45,9 @@
   width: 100%;
   height: 100%;
   z-index: 1;
-  background: radial-gradient(circle at center 35%, rgba(212, 140, 55, 0.15) 0%, rgba(0,0,0,0) 60%),
-              linear-gradient(to top, #110e14 0%, rgba(17, 14, 20, 0.85) 40%, rgba(17, 14, 20, 0.5) 100%),
-              linear-gradient(to right, rgba(17, 14, 20, 0.9) 0%, rgba(0,0,0,0) 25%, rgba(0,0,0,0) 75%, rgba(17, 14, 20, 0.9) 100%);
+  background: radial-gradient(circle at center 35%, rgba(17, 14, 20, 0.4) 0%, rgba(30, 15, 25, 0.7) 60%),
+              linear-gradient(to top, #110e14 0%, rgba(17, 14, 20, 0.9) 40%, rgba(17, 14, 20, 0.6) 100%),
+              linear-gradient(to right, rgba(201, 106, 50, 0.15) 0%, rgba(0,0,0,0) 25%, rgba(0,0,0,0) 75%, rgba(201, 106, 50, 0.15) 100%);
 }
 
 .heroContent {
@@ -72,39 +72,40 @@
 }
 
 .headlineLine1 {
-  font-size: 2rem;
-  letter-spacing: 0.1em;
-  font-weight: 300;
+  font-size: 1.5rem;
+  letter-spacing: 0.15em;
+  font-weight: 400;
   color: #fdfbf7;
-  margin-bottom: 0.25rem;
+  margin-bottom: 0.5rem;
+  text-transform: uppercase;
 }
 @media (min-width: 768px) {
-  .headlineLine1 { font-size: 2.5rem; }
+  .headlineLine1 { font-size: 2rem; }
 }
 
 .headlineLine2 {
-  font-size: 3rem;
+  font-size: 3.5rem;
   font-weight: 400;
   text-transform: uppercase;
   color: #fdfbf7;
-  line-height: 1.1;
+  line-height: 1;
 }
 @media (min-width: 768px) {
-  .headlineLine2 { font-size: 5rem; }
+  .headlineLine2 { font-size: 5.5rem; }
 }
 
 .scriptAccent {
   display: block;
   font-size: 4.5rem;
-  line-height: 0.6;
+  line-height: 0.7;
   color: #D4AF37;
   text-transform: none;
   font-weight: 400;
-  margin: 0.5rem 0;
-  transform: rotate(-2deg);
+  margin: 1rem 0;
+  transform: rotate(-3deg);
 }
 @media (min-width: 768px) {
-  .scriptAccent { font-size: 6.5rem; }
+  .scriptAccent { font-size: 7rem; margin: 1.5rem 0; }
 }
 
 /* 4. Decorative Divider */
@@ -148,10 +149,9 @@
   border: 1px solid #d4af37;
   color: #d4af37;
   padding: 0.5rem 1.5rem;
-  font-size: 0.75rem;
-  font-weight: 800;
-  letter-spacing: 0.2em;
-  text-transform: uppercase;
+  font-size: 0.85rem;
+  font-weight: 400;
+  letter-spacing: 0.1em;
   z-index: 20;
   white-space: nowrap;
 }
@@ -160,8 +160,10 @@
   display: grid;
   grid-template-columns: 1fr;
   width: 100%;
-  background: rgba(17, 14, 20, 0.85); /* unified background surface */
-  border: 1px solid #332b38;
+  background: rgba(17, 14, 20, 0.85);
+  border: 1px solid #d4af37;
+  outline: 1px solid rgba(212, 175, 55, 0.5);
+  outline-offset: -4px;
   backdrop-filter: blur(8px);
   position: relative;
 }
@@ -185,7 +187,7 @@
 }
 
 .choiceCard {
-  padding: 3rem 1.5rem;
+  padding: 3.5rem 1.5rem 3rem;
   display: flex;
   flex-direction: column;
   align-items: center;
@@ -205,16 +207,18 @@
   content: '';
   position: absolute;
   bottom: 0;
-  right: 0;
-  width: 100%;
+  right: 4px;
+  width: calc(100% - 8px);
   height: 1px;
   background: linear-gradient(to right, transparent, rgba(212, 175, 55, 0.3), transparent);
 }
 @media (min-width: 768px) {
   .choiceCardWrapper:not(:last-child)::after {
     width: 1px;
-    height: 100%;
-    top: 0;
+    height: calc(100% - 8px);
+    top: 4px;
+    right: 0;
+    bottom: auto;
     background: linear-gradient(to bottom, transparent, rgba(212, 175, 55, 0.3), transparent);
   }
   /* Remove right divider on the 2nd card in 2-col layout */
@@ -260,20 +264,25 @@
 
 .choiceCard:hover, .choiceCard:focus-visible {
   outline: none;
-  background: rgba(212, 175, 55, 0.05); /* subtle hover state that doesn't break the board */
+  background: rgba(212, 175, 55, 0.05);
+  box-shadow: inset 0 0 20px rgba(212, 175, 55, 0.05);
 }
 
 .illustration {
   margin-bottom: 2rem;
   opacity: 0.9;
-  transition: transform 0.4s ease;
+  transition: transform 0.4s ease, filter 0.4s ease;
   display: flex;
   align-items: center;
   justify-content: center;
+  height: 80px;
+}
+
+.illustration svg {
+  width: auto;
+  height: 100%;
 }
 
 .choiceCard:hover .illustration {
-  transform: scale(1.05);
+  transform: translateY(-2px);
+  filter: drop-shadow(0 0 8px rgba(212, 175, 55, 0.3));
 }
 
-.cardCity .illustration { color: #8da6bd; }
-.cardSwamp .illustration { color: #8c9e6e; }
-.cardPlantation .illustration { color: #ab799a; }
-.cardNotSure .illustration { color: #c98b61; }
+.cardCity .illustration { color: #5c6b73; }
+.cardSwamp .illustration { color: #6b705c; }
+.cardPlantation .illustration { color: #5c4d5c; }
+.cardNotSure .illustration { color: #b75d31; }
 
 .cardTitle {
-  font-size: 1.5rem;
+  font-size: 1.75rem;
   font-weight: 400;
   margin-bottom: 1rem;
   color: #fdfbf7;
@@ -293,7 +302,7 @@
 .cardCopy {
   font-size: 1rem;
   line-height: 1.6;
-  font-weight: 300;
+  font-weight: 400;
   color: rgba(253, 251, 247, 0.8);
   margin-bottom: 2rem;
   flex-grow: 1;
