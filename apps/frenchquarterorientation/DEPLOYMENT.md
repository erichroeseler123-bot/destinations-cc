# French Quarter Orientation — Standalone Vercel Deployment

Production domains: `frenchquarterorientation.com` and `www.frenchquarterorientation.com`

Required Vercel settings:
- Git repository: `erichroeseler123-bot/destinations-cc`
- Production branch: `main`
- Framework: Next.js
- Root Directory: `apps/frenchquarterorientation`
- Node.js: 24.x

Validate `/`, `/agent.json`, `/llms.txt`, `/robots.txt`, `/sitemap.xml`, and `/api/health` before promoting.

Do not deploy Welcome to the Swamp source into this project. French Quarter Orientation is a distinct first-hour New Orleans orientation property.
